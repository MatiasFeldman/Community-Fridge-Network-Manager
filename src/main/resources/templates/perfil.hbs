{{#partial "styles"}}
    <link rel="stylesheet" href="/css/perfil.css">
    <script src="/js/perfil.js" defer></script>
{{/partial}}

{{#partial "contenido"}}
    <div>
        <div class="d-flex justify-content-center flex-column gap-3">
            <h1 class="text-dark fw-bold fs-1 text-center">Perfil</h1>
            <picture class="w-100 text-center">
                <svg xmlns="http://www.w3.org/2000/svg" width="128" height="128" fill="dark"
                     class="bi bi-person-circle text-center" viewBox="0 0 16 16">
                    <path d="M11 6a3 3 0 1 1-6 0 3 3 0 0 1 6 0"/>
                    <path fill-rule="evenodd"
                          d="M0 8a8 8 0 1 1 16 0A8 8 0 0 1 0 8m8-7a7 7 0 0 0-5.468 11.37C3.242 11.226 4.805 10 8 10s4.757 1.225 5.468 2.37A7 7 0 0 0 8 1"/>
                </svg>
            </picture>
            <p class="text-dark mb-1 text-center fs-2 fw-semibold">{{usuario}} - Id: {{id}}</p>
            <div class="d-flex flex-row gap-2 justify-content-center align-items-center">
                {{#each roles}}
                    <p class="p-2 fs-6" id="{{this}}">{{this}}</p>
                {{/each}}
            </div>
            <div class="campos-usuario">
                {{#each humano.atributos}}
                    <div class="form-group">
                        <label class="text-dark" for="{{this.nombre}}">{{this.nombre}}</label>
                        <input id="{{this.nombre}}" name="{{this.nombre}}" type="{{this.tipo}}" value="{{this.valor}}"
                               placeholder="{{this.valor}}" {{#if this.constante}}disabled="disabled"{{/if}}>
                    </div>
                {{/each}}
            </div>
            <div class="d-flex mt-3 justify-content-center align-items-center gap-3">
                <button class="boton fw-bold w-100" id="cerrar">Cerrar sesion</button>
                <button data-id="{{id}}" class="boton fw-bold w-100" id="guardar">Guardar cambios</button>
            </div>
        </div>
    </div>
{{/partial}}
{{> templates/base }}